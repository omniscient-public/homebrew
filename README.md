# Homebrew Tap for Omni CLM

## Installation
Install the Omniscient CLI with 
```sh
brew install omniscient-public/cli/omni-cli
```

## Usage
Once installed, you can check that the CLI was successfully installed with
```sh
omni-cli -V
```
